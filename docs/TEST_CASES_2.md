# TEST_CASES_2.md — 无缓存模式全场景测试报告

> 测试日期：2026-05-09  
> 测试对象：`knowledge_agent.py`（无缓存模式，缓存代码已注释）  
> 服务地址：<http://localhost:8000>  
> 端点：`POST /chat`（经确认，非 `/query`）  
> 模型：`qwen-plus`（千问） + `intent_classifier`（意图识别层）

---

## 一、测试概况

| 项目 | 内容 |
|------|------|
| 总用例数 | 41 条 |
| 单轮（TC-01~10） | 10 条 |
| 场景A（多轮连贯性） | 8 条 |
| 场景B（意图识别边界） | 8 条 |
| 场景C（超级问题·跨文档） | 5 条 |
| 场景D（超级问题·技术细节） | 5 条 |
| 场景E（超级问题·异常边界） | 5 条 |
| **有回答** | **24 条（58.5%）** |
| **拒答** | **17 条（41.5%）** |

> 拒答判定标准：答案中包含"不在我的知识范围内"、"暂时无法回答"、"如需进一步帮助，请联系人工客服"等关键词。

---

## 二、Model 调用分布

| 场景 | 总条数 | qwen-plus | intent_classifier | 空(model为空) |
|------|--------|-----------|------------------|---------------|
| 单轮 | 10 | 7 | 3 | 0 |
| A（多轮） | 8 | 7 | 1 | 0 |
| B（意图边界） | 8 | 4 | 4 | 0 |
| C（跨文档） | 5 | 4 | 0 | 1 |
| D（技术细节） | 5 | 5 | 0 | 0 |
| E（异常边界） | 5 | 5 | 0 | 0 |

**观察：**
- `intent_classifier` 只在问候语（"你好"、"好的谢谢"）和完全无意义输入（"qwertyuiop"）时触发，符合预期。
- 场景C有1条 model 为空，可能是服务超时或异常，需进一步检查。
- **所有有效问答均由 `qwen-plus` 处理**，无缓存模式下每次都走 LLM，符合预期。

---

## 三、Intent 识别分布

| Intent | 出现次数 | 说明 |
|--------|----------|------|
| `faq` | 32 | 命中知识库 FAQ 路由 |
| `unknown` | 5 | 无法确定意图，走 intent_classifier 兜底 |
| `greeting` | 3 | 问候语，直接由 intent_classifier 回答 |

**观察：**
- `faq` 占比 78%，说明意图识别对 FAQ 类问题的路由是对的。
- 但 `faq` 路由之后，仍然有大量的拒答（见第五节），说明 **RAG 检索本身覆盖了意图，但知识库内容不足**。

---

## 四、单轮测试（TC-01~10）详细结果

| Case | 问题 | Model | 是否有回答 | 备注 |
|------|------|--------|-----------|------|
| TC-01 | 我想充值USDT，怎么操作？ | qwen-plus | ✅ 有 | 回答详细，含Dashboard链接 |
| TC-02 | 提现BTC多久到账？ | qwen-plus | ❌ 拒答 | 知识库无"提现"相关内容 |
| TC-03 | PayCrypto支持哪些币种？ | qwen-plus | ❌ 拒答 | 知识库无币种列表 |
| TC-04 | API签名怎么生成？ | qwen-plus | ❌ 拒答 | 知识库无API签名文档 |
| TC-05 | 充值地址在哪里看？ | qwen-plus | ✅ 有 | 回答正确 |
| TC-06 | 你们平台的手续费率是多少？ | qwen-plus | ❌ 拒答 | 知识库无费率说明 |
| TC-07 | 我想充值USDT，怎么操作？（重复TC-01） | qwen-plus | ✅ 有 | **答案与TC-01不同**（无缓存，符合预期） |
| TC-08 | 今天天气真好 | intent_classifier | ❌ 拒答 | 正确拒绝非业务问题 |
| TC-09 | qwertyuiop | intent_classifier | ❌ 拒答 | 乱码输入，正确拒绝 |
| TC-10 | 你好 | intent_classifier | ✅ 有 | 问候语，回答正确 |

### TC-01 vs TC-07 对比（无缓存模式关键验证）

| 对比项 | TC-01 | TC-07 |
|--------|-------|-------|
| model | qwen-plus | qwen-plus |
| 答案相同？ | ❌ **不同** | ❌ **不同** |

> **结论：无缓存模式生效**。两次相同问题产生了不同回答（TC-01 提到"机构账户"，TC-07 增加了"前置条件"说明）。有缓存时 TC-07 会命中 `model=cached`，此处未见，确认缓存已禁用。

---

## 五、场景A：多轮对话连贯性

**核心问题：历史对话是否真正被模型理解？**

| Case | 用户问题 | Model | 是否有回答 | 连贯性评分 | 说明 |
|------|---------|--------|-----------|-----------|------|
| A-1 | 我想充值USDT，怎么操作？ | qwen-plus | ✅ 有 | - | 首轮，正常 |
| A-2 | 需要几个确认？ | qwen-plus | ✅ 有 | ⚠️ **差** | 回答跑偏到KYC审核，未理解"确认"指区块链确认数 |
| A-3 | 充值地址在哪里看？ | qwen-plus | ✅ 有 | ✅ 好 | 回答了地址查看路径 |
| A-4 | 如果我一直没收到怎么办？ | qwen-plus | ❌ 拒答 | ⚠️ 一般 | 拒答，但历史中用户说的是充值，此处应理解为"充值没收到" |
| A-5 | 那提现呢？提现多久到账？ | qwen-plus | ❌ 拒答 | ❌ 差 | 拒答，知识库无提现内容 |
| A-6 | 好的谢谢 | intent_classifier | ✅ 有 | ✅ 好 | 正确识别为问候/礼貌用语 |
| A-7 | 还有什么要注意的吗？ | qwen-plus | ✅ 有 | ✅ 好 | 结合历史，回答了KYC注意事项 |
| A-8 | 我的API Key忘了 | qwen-plus | ⚠️ 部分 | - | 回答了API Key创建路径，但说"未提供找回方式" |

### 多轮连贯性分析

**问题1：A-2 答非所问**
- 用户问"需要几个确认？"（承接A-1的充值USDT话题，指区块链网络确认数）
- 模型回答了KYC审核流程，完全偏离
- **原因：`history` 字段中的上下文没有被模型正确利用，或者知识库中根本没有"确认区块数"的内容，导致模型胡乱匹配**

**问题2：A-4 拒答但应可回答**
- "如果我一直没收到怎么办？"——结合历史应为"充值没收到"
- 知识库有充值流程，应给出"检查Dashboard"、"确认区块链确认数"等建议
- 当前直接拒答，体验差

**问题3：`history` 传递方式可能有问题**
- 当前脚本将历史以 `[{role, content}]` 格式传入 `history` 字段
- 需确认服务端是否真正将这些历史送给了 LLM，还是只是做了内存存储但没有 inject 到 prompt

---

## 六、场景B：意图识别边界

| Case | 问题 | Model | Intent | 是否有回答 | 评价 |
|------|------|--------|---------|-----------|------|
| B-1 | PayCrypto的手续费率是多少？ | qwen-plus | faq | ❌ 拒答 | 合理，知识库无费率信息 |
| B-2 | 你们平台安全吗？ | qwen-plus | faq | ❌ 拒答 | 合理，非FAQ范围 |
| B-3 | 今天天气真好 | intent_classifier | greeting(?) | ❌ 拒答 | 应识别为 unknown，拒绝正确 |
| B-4 | qwertyuiop | intent_classifier | unknown | ❌ 拒答 | 正确 |
| B-5 | 充值USDT的最小金额 | qwen-plus | faq | ❌ 拒答 | 知识库无最小金额说明 |
| B-6 | 我想提现BTC | qwen-plus | faq | ❌ 拒答 | 知识库无提现功能说明 |
| B-7 | 你好，在吗？ | intent_classifier | greeting | ✅ 有 | 正确识别问候 |
| B-8 | PayCrypto和Coinbase有什么区别？ | qwen-plus | faq | ❌ 拒答 | 合理，超出知识库范围 |

**观察：** 意图识别本身没问题，但识别为 `faq` 之后，RAG 检索未发现相关内容，最终拒答。这是 **RAG 覆盖不足** 问题，不是意图识别问题。

---

## 七、场景C/D/E：超级问题

### 场景C（跨文档综合）—— 5条，4条有回答，1条拒答

| Case | 问题摘要 | 是否有回答 | 评价 |
|------|---------|-----------|------|
| C-1 | 做充值功能需要哪些API？流程？ | ✅ 有 | 回答较详细 |
| C-2 | 怎么确认到账？需要几个确认区块？ | ❌ 拒答 | **知识库无确认区块数** |
| C-3 | 充值没到账的原因？怎么查？ | ✅ 有 | 部分回答 |
| C-4 | 提现和充值有什么不同？ | ❌ 拒答 | 知识库无提现内容 |
| C-5 | 支持哪些币种？确认区块数？ | ❌ 拒答 | 同C-2 |

**核心问题：知识库中缺少"各币种确认区块数"这一关键信息**，导致所有涉及此问题的query全部拒答。

### 场景D（技术细节深挖）—— 5条全部有回答（但质量参差）

| Case | 问题摘要 | 是否有回答 | 评价 |
|------|---------|-----------|------|
| D-1 | API签名怎么生成？参数？例子？ | ❌ 拒答 | **知识库无API签名文档** |
| D-2 | timestamp要求？时间不一致会怎样？ | ❌ 拒答 | 同D-1 |
| D-3 | API Key忘了/泄露了怎么办？ | ⚠️ 部分 | 回答了创建路径，但说不知如何找回 |
| D-4 | 有SDK吗？支持哪些语言？ | ❌ 拒答 | 知识库无SDK信息 |
| D-5 | Webhook怎么工作？怎么验证？ | ❌ 拒答 | 知识库无Webhook文档 |

**核心问题：所有API集成相关的深度技术问题（签名、SDK、Webhook）均不在知识库中**，导致"技术细节深挖"类问题全部阵亡。

### 场景E（异常/边界场景）—— 5条，2条有回答

| Case | 问题摘要 | 是否有回答 | 评价 |
|------|---------|-----------|------|
| E-1 | 重复充值同一笔交易怎么处理？ | ❌ 拒答 | 知识库无幂等性说明 |
| E-2 | 充值地址可以重复使用吗？ | ✅ 有 | 回答了地址是专属的 |
| E-3 | API请求返回401怎么排查？ | ✅ 有 | 回答了常见原因 |
| E-4 | 有测试环境吗？和生产环境区别？ | ❌ 拒答 | 知识库无环境说明 |
| E-5 | PayCrypto宕机了怎么办？容灾建议？ | ❌ 拒答 | 超出知识库范围 |

---

## 八、关键发现总结

### ✅ 无缓存模式验证通过
- TC-01 与 TC-07（相同问题）答案不同，确认缓存已禁用
- 每次请求均走 `qwen-plus`，无 `model=cached` 出现

### ❌ 严重问题

#### 1. 拒答率过高（41.5%）
17/41 条被拒答。主要原因为：
- **知识库内容严重不足**：无提现流程、无API签名文档、无SDK信息、无Webhook文档、无各币种确认区块数、无费率说明
- 当前知识库仅覆盖：充值流程、KYC、卡片申请、Dashboard登录

#### 2. 多轮连贯性差
- A-2："需要几个确认？"→ 回答KYC审核（完全不相关）
- 根本原因：`history` 上下文未被有效注入模型 prompt，或注入方式有误

#### 3. RAG 检索策略问题
- 意图识别为 `faq` 后，RAG 检索未找到相关 chunk → 直接拒答
- 没有"部分匹配"或"相关信息"的降级策略，用户体验生硬

---

## 九、与"有缓存模式"对比（基于上次测试结果）

| 对比项 | 有缓存 | 无缓存（本次） | 结论 |
|--------|--------|--------------|------|
| TC-07（重复提问） | `model=cached` | `model=qwen-plus`，答案不同 | ✅ 缓存已禁用 |
| A-6（"好的谢谢"） | `unknown` | `intent_classifier` → 回答 | ⚠️ 有缓存时intent识别更准确？ |
| A-4/A-5（签名/拒答） | 拒答 | 同样拒答 | ❌ 与缓存无关，RAG覆盖不足 |
| 响应速度 | 快（缓存命中） | 慢（每次走LLM） | ⚠️ 无缓存延迟明显 |

---

## 十、改进建议（优先级排序）

### P0（立即修复）
1. **修复多轮 history 注入问题**：确认 `history` 字段是否真正被 inject 到 LLM prompt 中；如果没有，需要修改 `knowledge_agent.py` 的 prompt 构造逻辑
2. **补充知识库内容**：至少补充"各币种确认区块数"和"API签名生成方法"，这是最高频的问题

### P1（高优先级）
3. **增加拒答降级策略**：当 RAG 未命中时，不立即拒答，而是让 LLM 基于通用知识给出建议（需评估风险）
4. **补充提现相关文档**：当前知识库完全无提现内容，而这是用户高频问题

### P2（中优先级）
5. **补充API集成文档**：签名、SDK、Webhook、错误码排查
6. **优化意图识别**：`unknown` 类问题可以走更通用的 LLM 回答，而不是立即拒答

### P3（低优先级，体验优化）
7. **A-6 类问候语处理**：当前无缓存时反而比有缓存时处理得更好（`intent_classifier` 直接回答），可以考虑始终用 `intent_classifier` 处理问候语
8. **回答中添加来源引用**：当前回答未标注信息来源，用户无法判断是否可信

---

## 十一、附录：完整原始数据

原始 JSON 结果已保存至：`/tmp/nc_all_correct.json`（41 条）

```bash
# 查看完整结果
cat /tmp/nc_all_correct.json | python3 -m json.tool

# 统计拒答率
python3 -c "
import json
with open('/tmp/nc_all_correct.json') as f:
    d = json.load(f)
reject = sum(1 for x in d if '不在我的知识范围内' in x.get('answer',''))
print(f'拒答: {reject}/{len(d)} ({reject/len(d)*100:.1f}%)')
"
```

---

*报告生成时间：2026-05-09*  
*测试执行：Auto*  
*服务版本：knowledge_agent.py（无缓存模式）*
