# 2026-05-02 工作日志

## 知识库支持 Markdown 智能分块

升级 `rag_knowledge_base.py` 的 `add_file` 方法：
- `.md` 文件按 `#/##/###` 标题结构分块，每节保留 `heading` metadata
- 无标题的 md 退回纯文本分块
- 新增演示文件 `data/knowledge/产品手册.md`

## mem0-demo 服务启动成功

- 安装了 Python 3.11.9（系统 Python 3.9 不满足 mem0ai 要求）
- 修正依赖：dashscope 改为 `>=1.20.0`，chromadb 升级到 1.5.8，新增 python-multipart
- 修复 FastAPI `on_event` 废弃问题，改为 lifespan 方式
- seed 知识库改为异步后台任务，避免 API Key 无效阻塞启动
- 启动命令：`/Users/native/.workbuddy/binaries/python/versions/3.11.9/bin/python3 main.py`
- 服务地址：http://localhost:8000，API 文档：http://localhost:8000/docs
- 当前 .env 中 API Key 为示例值，需替换为真实 DashScope Key 才能正常对话

## 前端 Markdown 渲染支持

- 引入 `marked.js`（CDN: cdn.jsdelivr.net/npm/marked/marked.min.js）
- 修改 `appendMessage()`：bot 回复用 `marked.parse()` 渲染，用户消息保持纯文本
- 新增 Markdown 内容 CSS 样式（代码块、引用、表格、加粗、列表等）
- `agent.py` System Prompt 加入 Markdown 输出指令（标题/加粗/列表/代码块/引用/表格）
- `.env.example` 修正了错误的 API Key 格式提示（原示例填的是 Anthropic Key）

## 对话历史存档功能

- 新增 `add_conversation_archive()` 方法：绕过 LLM 提取，直接用 ChromaDB client 写入独立 collection `conv_archive`
- `chat()` 方法末尾自动存档（格式：`【时间】\n用户：xxx\n助手：xxx`）
- `get_all_memory()` / `search_memory()` / `delete_user_memory()` / `delete_memory_by_id()` 均已兼容对话存档
- 前端：对话记录类型 badge 紫色（`#ede9fe`），列表截断 80 字 + ▾ 展开按钮

## 记忆详情 + 历史记录功能

- `memory_manager.py`：新增 `get_memory_by_id()` / `get_memory_history()` / `update_memory()` 方法
- `main.py`：新增 `GET /memory/{user_id}/{memory_id}`（详情）、`GET /memory/{user_id}/{memory_id}/history`（历史）、`PUT /memory/{user_id}/{memory_id}`（更新）三个路由（放在 DELETE 前避免路径冲突）
- `static/index.html`：新增记忆详情弹窗（内容和历史两个 Tab），记忆条目点击打开详情，删除按钮用 stopPropagation 阻止冒泡；历史记录用时间线展示，支持 ADD（绿色）、UPDATE（橙色）、DELETE（红色）三种事件

## 输入框快捷键对调

- `static/index.html`：`handleKey()` 逻辑从 `Enter 发送 / Shift+Enter 换行` 改为 `Enter 换行 / Shift+Enter 发送`
- 提示文字同步更新

## Custom Instructions 功能

- **config.py**：新增 `MEM0_CUSTOM_INSTRUCTIONS`（环境变量）和 `MEMORY_STRATEGY`（预置策略选择）配置项
- **memory_manager.py**：
  - 新增三个预设策略：`CUSTOMER_SERVICE_INSTRUCTIONS`（客服）、`GENERAL_INSTRUCTIONS`（通用）、`STRICT_INSTRUCTIONS`（严格）
  - `build_mem0_config()` 支持 custom_instructions 参数注入 mem0 全局配置
  - `switch_strategy()`：运行时重建 Memory 实例切换策略
  - `set_custom_instructions()`：运行时设置完全自定义指令
  - `get_current_strategy()`：返回当前策略（基于关键词判断）
  - **踩坑**：mem0 0.1.29 的 `add()` 方法虽有 `prompt` 参数但未实现，运行时覆盖只能通过重建 Memory 实例
- **main.py**：新增4个 API：`GET/PUT /memory/strategy`、`PUT /memory/instructions`、`POST /memory/test-instructions`
- **agent.py**：更新 system prompt，与 CUSTOMER_SERVICE_INSTRUCTIONS 提取要求对齐
- 验证：customer_service 正确提取订单/物流/售后信息；strict 过滤闲聊无额外记忆
